'use client';

import styles from './principal.module.css';

export default function pt-BR.TextInfo.ToTitleCase(principal))Page() {
  return (
    <div className={styles.container}>
      <h1>pt-BR.TextInfo.ToTitleCase(principal))</h1>
      <p>Página do NutriSense</p>
    </div>
  );
}
