'use client';

import styles from './login.module.css';

export default function pt-BR.TextInfo.ToTitleCase(login))Page() {
  return (
    <div className={styles.container}>
      <h1>pt-BR.TextInfo.ToTitleCase(login))</h1>
      <p>Página do NutriSense</p>
    </div>
  );
}
